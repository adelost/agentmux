<?xml version="1.0" encoding="utf-8"?>
<!--
  A blocked loop: the one phase that needs a person to do something, so it wears the shape everything
  else on the phone uses for that. The status bar never shows it in practice, because a blocked loop has
  no service to post a notification; it exists so the declared glyph is total and no surface has to
  choose one natively.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M12,3.5L22,20.5L2,20.5Z"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:strokeWidth="2.2" />
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M12,10L12,15"
        android:strokeColor="#FFFFFFFF"
        android:strokeLineCap="round"
        android:strokeWidth="2.2" />
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M12,17.2m-1.15,0a1.15,1.15 0,1 1,2.3 0a1.15,1.15 0,1 1,-2.3 0" />
</vector>
