# Link Studio port boundary

The Link ProductSpec package is pinned to public `0.3.78` (SHA-256 `b1f1094bdcd5672c70e0f80b71be628a78bc2c27aa37e1ce91b09c50e2f99b7b`). ProductEmit `0.1.49` supplies the matching ProductSpec peer type; `generate.ts` imports Node-only manifest tools from ProductSpec `/node`. `npm run build`, `npm test` (9/9), and `npm run check-generated` passed. Regeneration changed the product JSON version, not existing Kotlin product behavior.

The Link emitter now generates one port-return hook from the compiled binding registry for debug and a no-op release variant. `LinkProductPortRuntime.send` and `.emit` call it only after their real sink returns, before publishing the connected state. `LinkProductGraph` owns the optional observer handle and clears it on close. No manually chosen port IDs or hand-written trace events are in product runtime code.

The focused real-graph test was red before the `.emit` hook (expected `capture.talk.command`, got no events) and green after. The focused typed `.send` test was red with that hook removed and green when restored. `v1d-studio record --product amux-link` ran the real graph test and wrote **one** test-run port event, `port::capture.talk.command`; `v1d-studio laws` passed **32/32**, and `converge` said Converged for loaded evidence. This is no finite-logic comparison and no live device observation. The image at `/home/adelost/lsrc/.artifacts/link-studio-ui-2026-09-24/trace-test-run.png` was opened and checked: AMUX Link selected, TEST RUN, the named port, 1 event, 0 dropped, and no browser console errors.

Both `:app:assembleRelease` and `:wear:assembleRelease` passed. An exact `classes*.dex` string scan found `V1D_STUDIO_TRACE_DIR` and `Link Studio test trace unavailable` in both debug APKs, and **neither** string, `/runtime/v1`, `v1d-runtime.v1` nor `studio-observation/ticket` in either release APK. The local unsigned release APKs are not a published update claim. A generated release no-op remains callable, but no test-file or live transport is packaged.

`v1d-studio live connect --device SERIAL` is presently SKYVW-specific: its CLI fixes the package to `com.adelost.skydivealtimeter`, the action to `com.adelost.skydivealtimeter.STUDIO_OBSERVE`, and the product to `skyvw`. Pointing it at the attached Wear emulator would stage a ticket for the wrong application. No Link emulator live capture or live screenshot is claimed. The safe next seam, if required, is explicit Link target support in Studio's connector plus a Link debug transport, not a manual adb ticket or a pretend port event.
